name: Build Android AAB

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Unzip project
        run: |
          unzip -o account_recovery_consultant.zip -d extracted_app

      - name: Force Fix Dependencies & Install
        run: |
          PROJECT_DIR=$(find extracted_app -name "pubspec.yaml" -exec dirname {} \;)
          cd "$PROJECT_DIR"
          echo -e "\ndependency_overrides:\n  collection: ^1.18.0" >> pubspec.yaml
          flutter pub get

      - name: Build AAB
        run: |
          PROJECT_DIR=$(find extracted_app -name "pubspec.yaml" -exec dirname {} \;)
          cd "$PROJECT_DIR"
          flutter build appbundle --release --no-codesign

      - name: Upload AAB Artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-release-aab
          path: extracted_app/**/build/app/outputs/bundle/release/*.aab
